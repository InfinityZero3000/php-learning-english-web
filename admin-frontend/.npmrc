# .npmrc – admin-frontend
# Dùng legacy-peer-deps để tránh conflict peer dep của @testing-library/user-event.
# Đây là giá trị hợp lệ vì chúng ta đã explicit install @testing-library/dom@^10.
legacy-peer-deps=true
