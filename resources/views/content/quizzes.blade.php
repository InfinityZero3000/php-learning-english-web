@extends('layouts.app') @section('title','Quiz trong bài học') @section('top','Quản lý nội dung')
@section('content')<div class="hero"><h1>{{$lesson->title}}</h1><p>Quản lý quiz và câu hỏi trong bài học.</p></div><div class="wrap"><div class="toolbar"><div class="crumb"><a href="{{route('content.index')}}">Bài học</a> › {{$lesson->title}}</div><button class="btn" onclick="modal('quizModal')">＋ Thêm quiz</button></div><div class="grid">@forelse($quizzes as $quiz)<a class="card link" href="{{route('content.questions',[$lesson,$quiz])}}"><h3>{{$quiz->title}}</h3><p class="metric">🧩 {{$quiz->questions_count}} câu hỏi</p><p class="metric">⏱ {{$quiz->time_limit ?? 'Không giới hạn'}} phút　•　🎯 {{$quiz->pass_score ?? 60}} điểm đạt</p></a>@empty<div class="card">Chưa có quiz nào.</div>@endforelse</div></div><div id="quizModal" class="modal"><form class="dialog" onsubmit="toast('Đã lưu quiz');modal('quizModal');return false"><h2>Thêm quiz</h2><input required placeholder="Tên quiz"><input type="number" placeholder="Giới hạn thời gian (phút)"><input type="number" min="0" max="100" placeholder="Điểm đạt"><button class="btn">Lưu quiz</button></form></div>@endsection
