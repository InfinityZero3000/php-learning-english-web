import{fireEvent,render,screen,waitFor}from"@testing-library/react";import{describe,it,vi}from"vitest";import{VerifyEmailPage}from"./verify-email-page";import{auth}from"@/lib/api";vi.mock("next/navigation",()=>({useSearchParams:()=>new URLSearchParams("email=a%40b.test")}));vi.mock("./auth-layout",()=>({AuthLayout:({children}:{children:React.ReactNode})=><>{children}</>}));vi.mock("@/lib/api",async i=>{const a=await i<typeof import("@/lib/api")>();return{...a,auth:{...a.auth,resendVerification:vi.fn()}}});describe("VerifyEmailPage",()=>{it("resends verification",async()=>{vi.mocked(auth.resendVerification).mockResolvedValue({message:"ok"});render(<VerifyEmailPage/>);fireEvent.click(screen.getByRole("button",{name:"Resend verification email"}));await waitFor(()=>screen.getByRole("status"))})});
