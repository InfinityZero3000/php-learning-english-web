import { fireEvent,render,screen,waitFor } from "@testing-library/react";import{describe,it,vi}from"vitest";import{ForgotPasswordPage}from"./forgot-password-page";import{auth}from"@/lib/api";vi.mock("./auth-layout",()=>({AuthLayout:({children}:{children:React.ReactNode})=><>{children}</>}));vi.mock("@/lib/api",async i=>{const a=await i<typeof import("@/lib/api")>();return{...a,auth:{...a.auth,forgotPassword:vi.fn()}}});describe("ForgotPasswordPage",()=>{it("shows privacy-safe success",async()=>{vi.mocked(auth.forgotPassword).mockResolvedValue({message:"ok"});render(<ForgotPasswordPage/>);fireEvent.change(screen.getByLabelText("Email"),{target:{value:"a@b.test"}});fireEvent.click(screen.getByRole("button",{name:"Send reset link"}));await waitFor(()=>screen.getByRole("status"));})});
