import{render,screen}from"@testing-library/react";import{describe,it,vi}from"vitest";import{ResetPasswordPage}from"./reset-password-page";vi.mock("next/navigation",()=>({useRouter:()=>({replace:vi.fn()}),useSearchParams:()=>new URLSearchParams()}));vi.mock("./auth-layout",()=>({AuthLayout:({children}:{children:React.ReactNode})=><>{children}</>}));describe("ResetPasswordPage",()=>{it("does not submit an incomplete link",()=>{render(<ResetPasswordPage/>);screen.getByRole("alert");screen.getByRole("link",{name:"Request a new link"})})});
